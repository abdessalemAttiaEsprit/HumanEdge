package tn.esprit.backend.entities.Enum;

/**
 * GENERAL : compétence transversale (ex: communication). SPECIFIC : liée au métier/département de
 * l'employé — voir SkillCatalog pour les suggestions associées à chaque catégorie.
 */
public enum SkillCategory {
    GENERAL,
    SPECIFIC
}
